import{Router}from"express";import type{ResultSetHeader}from"mysql2";import{z}from"zod";import{pool}from"../db/pool.js";import{asyncHandler}from"../lib/async-handler.js";const router=Router();router.post("/",asyncHandler(async(req,res)=>{const input=z.object({content:z.string().trim().min(10).max(1000),contactPhone:z.string().regex(/^1[3-9]\d{9}$/).optional().or(z.literal("")),imageUrls:z.array(z.url()).max(5).default([])}).parse(req.body);const[result]=await pool.execute<ResultSetHeader>("INSERT INTO feedback (user_id,content,contact_phone,image_urls) VALUES (?,?,?,?)",[req.auth!.userId,input.content,input.contactPhone||null,JSON.stringify(input.imageUrls)]);res.status(201).json({success:true,data:{id:result.insertId,status:"pending"}})}));export default router;
